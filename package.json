{
  "name": "telegram-account-shop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "init-db": "node scripts/init-db.js",
    "set-webhook": "node scripts/set-webhook.js",
    "lint": "node --check api/telegram.js && node --check api/health.js && node --check lib/db.js && node --check lib/services.js && node --check scripts/load-env.js && node --check scripts/init-db.js && node --check scripts/set-webhook.js"
  },
  "dependencies": {
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "vercel": "^34.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
